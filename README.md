Dotfiles without sensitive info, for your perusal.

The vimrc file requires [vim-plug](https://github.com/junegunn/vim-plug) to be
installed. Once you've done that, call `:PlugInstall` to install the plugins.
